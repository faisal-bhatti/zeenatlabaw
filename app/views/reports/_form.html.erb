<%= form_for @report, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label "Report id", :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :rep_id, :class => 'text_field' %>
    </div>
  </div>
  <!-- <div class="control-group">
    <%#= f.label :grade, :class => 'control-label' %>
    <div class="controls">
      <%#= f.number_field :grade, :class => 'number_field' %>
    </div>
  </div> -->

   <p style="margin-left: 15%;">
    <%= f.file_field :file %>
  </p>
  <!-- <p> -->
    <%#= f.label :remote_file_url, "or file URL" %><br />
    <%#= f.text_field :remote_file_url %>
  <!-- </p> -->

  <div class="form-actions">
    <%= f.submit "Upload Report", :class => 'btn btn-primary' %>
    <%#= link_to t('.cancel', :default => t("helpers.links.cancel")),
                subjects_path, :class => 'btn' %>
  </div>
<% end %>